import 'package:flutter/material.dart';

const backgroundColor = Color(0xff0e0e0e);
const primaryColor = Colors.white;
final secondaryColor = Colors.grey[600]; // [600] 은 런타임에 색상이 계산되기 때문에 buildTime 상수인 const 사용불가